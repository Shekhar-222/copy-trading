fastapi==0.111.0
uvicorn[standard]==0.30.1
kiteconnect==5.0.1
pyotp==2.9.0
sqlalchemy==2.0.52  # 2.0.30 crashes importing on Python 3.14 ("Can't replace canonical symbol
# for '__firstlineno__'") - an internal conflict with a new Python 3.13+ class attribute that
# didn't exist when 2.0.30 was released. 2.0.52 has this fixed. Confirmed live, 2026-09,
# deploying to a fresh Python-3.14 Ubuntu server.
python-dotenv==1.0.0
requests==2.32.3
websockets==12.0
certifi>=2025.1.31,<2026.0.0  # growwapi>=1.5.0 requires this range; neo_api_client (below)
# downgrades it to an ancient 2022.12.7 as a side effect of installing - re-pin this back up
# afterward too, same drill as websockets. An outdated certifi means outdated root CA certs,
# which can start failing real HTTPS calls over time, not just a cosmetic pip warning.
pandas>=2.2.3,<3.0.0  # growwapi>=1.5.0 caps this at <3.0.0 (a real breaking major version, not
# just a strict-pin nuisance - don't let this float past it). CRITICAL: neo_api_client (below)
# downgrades this to an exact pandas==2.2.3, which on Python 3.14 has no pre-built wheel and
# gets compiled from source - that locally-compiled 2.2.3 build has a real, 100%-reproducible
# segfault in its pandas_datetime C extension whenever touched from more than one thread at
# once (confirmed live, 2026-09: crashed the entire backend process - not a Python exception,
# a SIGSEGV visible in `dmesg`, at the identical code offset every time - inside this app's own
# ThreadPoolExecutor child-replication workers, which is exactly how every real order runs).
# Re-pin to this range (not exactly 2.2.3) after installing neo_api_client, same drill as
# websockets/certifi - resolves to a real prebuilt-wheel release (2.3.3 at time of writing) with
# no source compile and no crash.
pydantic==2.13.5  # growwapi>=1.5.0 requires >=2.10.2; fastapi==0.111.0 accepts it fine too.
# Pinned at 2.13.5 specifically (not the 2.10.2 floor) because pydantic-core has no pre-built
# wheel for Python 3.14 below this - anything older tries to compile from source via Rust/PyO3,
# which fails outright ("configured Python interpreter version (3.14) is newer than PyO3's
# maximum supported version") since PyO3 hadn't added 3.14 support yet at that pydantic-core
# release. Confirmed live, 2026-09, deploying to a fresh Python-3.14 Ubuntu server.
cryptography==42.0.8

# Kotak Neo broker support. Installed from git since it's not on PyPI:
#   pip install "git+https://github.com/Kotak-Neo/Kotak-neo-api-v2.git@v2.0.2#egg=neo_api_client"
# NOTE: neo_api_client's setup pins websockets==8.1, certifi==2022.12.7, numpy==2.1.0, and
# pandas==2.2.3, conflicting with the pins above (FastAPI/uvicorn's /ws/live endpoint needs the
# newer websockets; growwapi needs the newer certifi and pandas; the pandas==2.2.3 exact pin is
# actively dangerous on Python 3.14 - see the pandas comment above, it segfaults the whole
# process). A plain `pip install -r requirements.txt` in one shot fails outright on any
# reasonably current pip with "ResolutionImpossible" - it won't silently pick one and let you
# fix it after like older pip versions did. Confirmed live, 2026-09: install everything else
# first, then neo_api_client on its own (letting it downgrade all four), then re-pin them back
# up - separate pip invocations, e.g.:
#   pip install -r <(grep -v neo_api_client requirements.txt | grep -v '^#')
#   pip install "git+https://github.com/Kotak-Neo/Kotak-neo-api-v2.git@v2.0.2#egg=neo_api_client"
#   pip install websockets==12.0
#   pip install "certifi>=2025.1.31,<2026.0.0"
#   pip install "pandas>=2.2.3,<3.0.0"
# Do NOT skip that last one - it's the segfault fix, not a cosmetic version bump.
# `pip check` afterward should report only neo-api-client's own four conflicting requirements
# (websockets==8.1, certifi==2022.12.7, numpy==2.1.0, pandas==2.2.3) - harmless, since this app
# never uses the SDK feature
# that needs them; any other reported conflict is a real problem worth investigating.
neo_api_client @ git+https://github.com/Kotak-Neo/Kotak-neo-api-v2.git@v2.0.2#egg=neo_api_client

# Angel One (SmartAPI) broker support - unlike Kotak Neo's SDK, this one is on PyPI.
# NOTE: the import name has changed across versions of this package (`from SmartApi import
# SmartConnect` on recent releases, `from smartapi import SmartConnect` on older ones) -
# angel_auth.py uses the newer `SmartApi` import; if it fails with ModuleNotFoundError, check
# which one the installed version actually provides. Also check, same as neo_api_client above,
# whether its own dependencies downgrade the websockets pin - reinstall websockets==12.0 after
# installing this if so.
smartapi-python==1.5.5
# smartapi-python==1.5.5 imports logzero at module load time but doesn't declare it as a pip
# dependency (confirmed on install: `from SmartApi import SmartConnect` raises
# ModuleNotFoundError: No module named 'logzero' without this line).
logzero==1.7.0
# smartapi-python also imports the 'websocket' module (from the websocket-client package) at
# load time via its own smartApiWebsocket.py, but likewise doesn't declare it - this stayed
# hidden for a long time because neo_api_client (Kotak, above) happens to depend on
# websocket-client too, so any install that included Kotak support masked the gap. Confirmed
# live, 2026-09, on a server that skipped installing neo_api_client: Angel One's own import
# chain fails on its own with `ModuleNotFoundError: No module named 'websocket'` without this.
websocket-client==1.8.0

# Groww broker support - on PyPI, all dependencies (pandas, protobuf, aiohttp, etc.) properly
# declared this time (confirmed on install - no undeclared-dependency gap like logzero above).
# NOTE: exceptions live in growwapi.groww.exceptions.BaseGrowwException (not "GrowwBaseException"
# as one doc page states - confirmed against the installed package's actual source).
growwapi==1.5.0
